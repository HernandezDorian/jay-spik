<ol class="items-list">
  {{#each spells as |spells spellLevel|}}
    <li class="item flexrow items-header">
      <div class="item-name">
        {{localize "JAY_SPIK.Item.Spell.SpellLVL" level=spellLevel}}
      </div>
      <div class="item-controls">
        <a
          class="item-control item-create"
          title="{{localize 'DOCUMENT.Create' type="Spell"}}"
          data-type="spell"
          data-spell-level="{{spellLevel}}"
        >
          <i class="fas fa-plus"></i>
          {{localize "JAY_SPIK.Item.Spell.AddLVL" level=spellLevel}}
        </a>
      </div>
    </li>
    {{#each spells as |item id|}}
      <li class="item flexrow" data-item-id="{{item._id}}">
        <div class="item-name flexrow">
          <div class="item-image">
            <a class="rollable" data-roll-type="item"><img
                src="{{item.img}}"
                title="{{item.name}}"
                width="24"
                height="24"
              /></a>
          </div>
          <h4>{{item.name}}</h4>
        </div>
        <div class="item-controls">
          <button
            type="button"
            class="spell-roll rollable"
            data-roll="{{item.system.formula}}"
            data-stat="{{item.system.stat}}"
            data-advantage="{{item.system.advantage}}"
            data-label="{{item.name}}"
          >
            <i class="fas fa-dice"></i>
            Lancer
          </button>
          {{#if item.system.damageFormula}}
            <button
              type="button"
              class="item-roll-damage rollable"
              data-item-id="{{item._id}}"
              title="Lancer les dégâts"
            >
              <i class="fas fa-dice-d20"></i>
              {{item.system.damageFormula}}
              {{#if item.system.armorPiercing}}<span
                  class="armor-piercing-indicator"
                  title="Perce-Armure"
                >🗲</span>{{/if}}
            </button>
          {{/if}}
          <a
            class="item-control item-edit"
            title="{{localize 'DOCUMENT.Edit' type="spell"}}"
          >
            <i class="fas fa-edit"></i>
          </a>
          <a
            class="item-control item-delete"
            title="{{localize 'DOCUMENT.Delete' type="spell"}}"
          >
            <i class="fas fa-trash"></i>
          </a>
        </div>
      </li>
    {{/each}}
  {{/each}}
</ol>